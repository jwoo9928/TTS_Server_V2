fastapi
uvicorn[standard]
soundfile
numpy
# PyTorch 2.x for CUDA 12.1 (compatible with CUDA 12.x toolkit)
torch --index-url https://download.pytorch.org/whl/cu121
# Add the 'dia' model package name here if it's installable via pip
# e.g., dia-model-package-name
